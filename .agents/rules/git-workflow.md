# Git & GitHub Workflow Rules

- **Always ask the user for explicit confirmation before making any changes in Git/GitHub.**
- Never run `git commit`, `git push`, branch deletions, or remote repository modifications without asking the user and obtaining their direct approval first.
